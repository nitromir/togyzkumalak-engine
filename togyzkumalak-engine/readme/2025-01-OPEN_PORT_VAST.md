# 🔓 Как открыть порт 8000 на Vast.ai

## Способ 1: Через настройки инстанса (РЕКОМЕНДУЕТСЯ)

1. **Открой свой инстанс** на Vast.ai
2. Найди раздел **"Config"** или **"Settings"**
3. Ищи **"Expose Ports"** или **"Port Mapping"** или **"Ports"**
4. Нажми **"Add Port"** или **"Expose Port"**
5. Введи:
   - **Port:** `8000`
   - **Type:** `TCP`
   - **Public Port:** `8000` (или любой другой, например 8080)
6. **Сохрани** изменения
7. **Перезагрузи инстанс** (Stop → Start)
8. Подожди 1-2 минуты

После этого открой в браузере:
```
http://151.237.25.234:8000
```
(или тот порт который указал в Public Port)

---

## Способ 2: Через Jupyter (если нет опции в UI)

Если в настройках нет опции открыть порт, используй **Jupyter Terminal**:

1. На Vast.ai нажми **"Open"** (зелёная кнопка)
2. В Jupyter: **New → Terminal**
3. Запусти сервер:
   ```bash
   cd /workspace/togyzkumalak/togyzkumalak-engine
   python run.py
   ```
4. Сервер будет доступен **только внутри Jupyter**

---

## Способ 3: Используй Jupyter для доступа к UI

1. На Vast.ai нажми **"Open"**
2. В Jupyter: **New → Notebook**
3. В ячейке выполни:
   ```python
   from IPython.display import IFrame
   IFrame('http://localhost:8000', width='100%', height=800)
   ```

Это откроет UI прямо в Jupyter!

---

## Способ 4: Измени порт сервера на стандартный (80 или 8080)

Если порт 8000 не открывается, можно изменить порт сервера:

В SSH сессии:
```bash
cd /workspace/togyzkumalak/togyzkumalak-engine
export PORT=8080
python run.py
```

Или отредактируй `run.py` чтобы использовать порт 8080.

---

## Рекомендация

**Самый простой способ:** Используй **Jupyter** (Способ 3) - не нужно открывать порты, всё работает сразу!
