# 🔧 SSH Troubleshooting - Vast.ai

## Проблема: Permission denied (publickey)

### Решение 1: Перезагрузи инстанс ⭐ (РЕКОМЕНДУЕТСЯ)

1. На Vast.ai нажми **"Stop"** на инстансе
2. Подожди 10 секунд
3. Нажми **"Start"**
4. Подожди 1-2 минуты пока загрузится
5. Попробуй подключиться снова:
   ```powershell
   ssh -p 23396 root@151.237.25.234 -L 8000:localhost:8000
   ```

### Решение 2: Проверь что ключ добавлен правильно

1. Иди на https://vast.ai/console/ssh-keys
2. Убедись что ключ там есть
3. Ключ должен начинаться с `ssh-rsa AAAAB3...`
4. Должна быть одна строка (без переносов)

### Решение 3: Используй Proxy SSH (если прямое не работает)

```powershell
ssh -p 16593 root@ssh7.vast.ai
```

Потом внутри:
```bash
ssh root@151.237.25.234 -p 23396
```

### Решение 4: Используй Jupyter Terminal (САМЫЙ ПРОСТОЙ!) ⭐⭐⭐

**Не нужен SSH ключ вообще!**

1. На Vast.ai нажми зелёную кнопку **"Open"**
2. В Jupyter: **New → Terminal**
3. Готово! Ты в терминале сервера

**Преимущества:**
- ✅ Работает сразу
- ✅ Не нужен SSH ключ
- ✅ Можно открыть несколько терминалов
- ✅ Можно загружать/скачивать файлы через UI

---

## Быстрая проверка

После перезагрузки инстанса попробуй:

```powershell
ssh -p 23396 root@151.237.25.234 -L 8000:localhost:8000
```

Если всё ещё не работает - используй **Jupyter Terminal** - это самый надёжный способ!
