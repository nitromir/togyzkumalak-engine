# 📖 FAQ: Руководство по обучению модели Тоғызқұмалақ

## 📋 Оглавление

1. [Обзор системы](#обзор-системы)
2. [Быстрый старт](#быстрый-старт)
3. [Вкладки интерфейса](#вкладки-интерфейса)
4. [Запуск дообучения](#запуск-дообучения)
5. [Рекомендуемые размеры датасета](#рекомендуемые-размеры-датасета)
6. [Параметры обучения](#параметры-обучения)
7. [Интерпретация результатов](#интерпретация-результатов)
8. [Источники данных](#источники-данных)
9. [Мониторинг прогресса](#мониторинг-прогресса)
10. [Автозагрузка модели](#автозагрузка-модели)
11. [Troubleshooting](#troubleshooting)
12. [FAQ](#faq)

---

## 🎯 Обзор системы

Система обучения Тоғызқұмалақ AI состоит из нескольких компонентов:

```
┌─────────────────────────────────────────────────────────────────┐
│                    🎮 Web-интерфейс (Frontend)                  │
├────────────┬────────────┬──────────────┬────────────┬──────────┤
│  🎯 Играть │ 📼 Реплеи  │ 🧠 Тренировка │ 🤖 Gemini  │ 📊 Аналитика │
│            │            │              │   Battle   │              │
└────────────┴────────────┴──────────────┴────────────┴──────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    🔧 Backend (FastAPI)                         │
├─────────────────────────────────────────────────────────────────┤
│  AIEngine  │  GymTrainer  │  GeminiBattle  │  MetricsCollector  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    📊 Данные и модели                           │
├─────────────────────────────────────────────────────────────────┤
│  models/   │   logs/     │   human_data/  │   gemini_summaries/ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🚀 Быстрый старт

### Шаг 1: Запуск сервера

```bash
cd gym-togyzkumalak-master/togyzkumalak-engine
..\venv\Scripts\activate  # Windows
python run.py
```

Сервер запустится на `http://localhost:8000`

### Шаг 2: Открыть веб-интерфейс

Откройте в браузере: `http://localhost:8000`

### Шаг 3: Выбрать режим обучения

Перейдите на нужную вкладку:
- **🧠 Тренировка** - Self-play обучение в Gym
- **🤖 Gemini Battle** - Игры против Gemini API
- **📊 Аналитика** - Мониторинг метрик и A/B тесты

---

## 🗂 Вкладки интерфейса

### 🎯 Играть
Играйте против AI разных уровней (1-5).

**Как использовать:**
1. Выберите уровень AI (1=случайный, 5=эксперт)
2. Кликните на лунку для хода
3. Наблюдайте за ходами AI

### 📼 Реплеи
Просмотр сохраненных игр.

**Как использовать:**
1. Выберите реплей из списка
2. Используйте ◀ ▶ для навигации по ходам
3. Анализируйте позиции

### 🧠 Тренировка (Self-Play)
Запуск обучения модели через самоигру в Gym-среде.

**Как использовать:**
1. Установите количество игр (рекомендуется: 100-1000)
2. Выберите размер batch
3. Настройте epsilon (исследование vs использование)
4. Нажмите "Запустить тренировку"
5. Следите за прогрессом в реальном времени

**Параметры:**
| Параметр | Описание | Рекомендация |
|----------|----------|--------------|
| Количество игр | Сколько игр сыграть | 500-1000 |
| Batch size | Размер батча для обучения | 32-64 |
| Learning rate | Скорость обучения | 0.001 |
| Epsilon | Вероятность случайного хода | 0.1-0.3 |

### 🤖 Gemini Battle
Игры вашей модели против Gemini API.

**Как использовать:**
1. Введите Gemini API ключ (если не настроен)
2. Установите количество игр
3. Выберите уровень модели (1-5)
4. Нажмите "Начать битву"
5. Наблюдайте за играми в реальном времени

**Важно:**
- Gemini timeout по умолчанию: 30 сек
- ELO рейтинг обновляется после каждой игры
- Резюме игр сохраняются в `logs/gemini_summaries/`

### 📊 Аналитика
Мониторинг обучения и A/B тестирование.

**Секции:**
1. **Статистика Gemini Battles** - Winrate, количество игр
2. **ELO Рейтинг** - Текущий рейтинг и категория
3. **Датасет** - Состав обучающих данных
4. **A/B Тесты** - Эксперименты с разными схемами

---

## 🎓 Запуск дообучения

### Метод 1: Через веб-интерфейс

#### Self-Play (вкладка "Тренировка")

```
1. Перейти на вкладку 🧠 Тренировка
2. Настроить параметры:
   - Количество игр: 500
   - Epsilon: 0.2 (для exploration)
3. Нажать "▶ Запустить тренировку"
4. Дождаться завершения
5. Модель автоматически сохранится
```

#### Gemini Battle (вкладка "Gemini Battle")

```
1. Перейти на вкладку 🤖 Gemini Battle
2. Настроить:
   - Количество игр: 20-50
   - Уровень модели: 5 (эксперт)
3. Нажать "🎮 Начать битву"
4. Игры и резюме сохраняются автоматически
```

### Метод 2: Через API

#### Запуск Self-Play через API

```bash
# Запустить тренировку
curl -X POST "http://localhost:8000/api/training/start" \
  -H "Content-Type: application/json" \
  -d '{
    "num_games": 500,
    "batch_size": 32,
    "epsilon": 0.2,
    "learning_rate": 0.001
  }'

# Проверить статус
curl "http://localhost:8000/api/training/status"

# Остановить тренировку
curl -X POST "http://localhost:8000/api/training/stop"
```

#### Запуск Gemini Battle через API

```bash
# Запустить битву
curl -X POST "http://localhost:8000/api/gemini-battle/start" \
  -H "Content-Type: application/json" \
  -d '{
    "num_games": 20,
    "model_level": 5
  }'

# Получить статистику
curl "http://localhost:8000/api/gemini-battle/stats"
```

### Метод 3: Через Python скрипт

```python
# scripts/run_training.py
import asyncio
from backend.gym_training import GymTrainingManager

async def run_full_training():
    trainer = GymTrainingManager()
    
    # Запустить 500 игр self-play
    result = await trainer.start_training(
        num_games=500,
        epsilon=0.2,
        batch_size=32
    )
    
    print(f"Обучено на {result['games_played']} играх")
    print(f"Финальный loss: {result['final_loss']}")

asyncio.run(run_full_training())
```

---

## 📊 Рекомендуемые размеры датасета

### Минимальные и оптимальные размеры

| Тип данных | Минимум | Оптимально | Максимум | Описание |
|------------|---------|------------|----------|----------|
| **Человеческие игры** | 500 | **2000-5000** | 50000+ | Качественные турнирные данные |
| **Self-Play игры** | 1000 | **10000-50000** | ∞ | Генерируются моделью |
| **Gemini Battle** | 50 | **200-500** | 1000+ | Для валидации и ELO |
| **Переходов (transitions)** | 50K | **200K-1M** | 10M+ | Отдельные ходы для обучения |

### Текущее состояние вашего датасета

- ✅ **~2300 игр** — хороший старт!
- ✅ **~246K переходов** — достаточно для первичного обучения
- 📈 Рекомендация: добавить 500+ игр Self-Play

### Правило 30-40-30

Для оптимального качества модели соблюдайте баланс:

```
📊 ИДЕАЛЬНЫЙ МИКС ДАННЫХ:
├─ 30-40% → Человеческие игры (качество, паттерны)
├─ 40-50% → Self-Play (новые позиции, улучшение)
└─ 10-20% → Gemini Battle (валидация, adversarial)
```

---

## ⚙️ Параметры обучения

### Настраиваемые параметры

| Параметр | Минимум | Рекомендуется | Максимум | Описание |
|----------|---------|---------------|----------|----------|
| **Эпохи** | 10 | **50-100** | 200 | Количество проходов по данным |
| **Batch Size** | 32 | **128** | 512 | Размер батча (больше = быстрее, но больше памяти) |
| **Learning Rate** | 0.0001 | **0.001** | 0.01 | Скорость обучения |

### Рекомендации по эпохам

- **10 эпох** — быстрый тест (~2 мин), Accuracy ~17%
- **50 эпох** — стандартное обучение (~10 мин), Accuracy ~40%
- **100 эпох** — глубокое обучение (~20 мин), Accuracy ~50%
- **200 эпох** — максимальное качество (~40 мин), риск переобучения

---

## 📈 Интерпретация результатов

### Ожидаемые метрики по эпохам

| Эпоха | Loss | Accuracy | Статус |
|-------|------|----------|--------|
| 1 | 2.2 | ~11% | Начало (случайный уровень) |
| 10 | 2.0 | ~17% | Модель начала учиться |
| 25 | 1.5 | ~30% | Хороший прогресс |
| 50 | 1.0 | ~40% | Качественная модель |
| 100 | 0.7 | ~50% | Отличный результат |

### Что означает Accuracy

- **11%** — случайный выбор (1 из 9 ходов)
- **20-30%** — модель различает хорошие/плохие ходы
- **40-50%** — сильная модель, сопоставима с любителем
- **50-60%** — отличная модель (люди тоже ошибаются!)
- **>60%** — возможно переобучение, проверьте на новых данных

### Что означает Loss

- **>2.0** — модель почти не обучена
- **1.5-2.0** — начальный прогресс
- **1.0-1.5** — хорошее обучение
- **0.5-1.0** — модель уверена в предсказаниях
- **<0.5** — возможно переобучение

---

## 💾 Автозагрузка модели

### Как это работает

При запуске сервера автоматически загружается **последняя версия** обученной модели:

```
models/
├── policy_net_human_v1.pt  ← первое обучение
├── policy_net_human_v2.pt  ← второе обучение
└── policy_net_human_v3.pt  ← загрузится автоматически!
```

### Где используется загруженная модель

- ✅ **Вкладка "Играть"** → уровень 5 (Эксперт)
- ✅ **Self-Play тренировка** → как базовая модель
- ✅ **Gemini Battle** → модель vs Gemini

### Ручная загрузка (если нужно)

```bash
curl -X POST "http://localhost:8000/api/model/load?name=policy_net_human_v2"
```

---

## 📚 Источники данных

### Типы данных для обучения

| Источник | Процент | Описание |
|----------|---------|----------|
| Турнирные данные людей | 30-40% | Качество, стиль игры |
| Self-play | 40-50% | Новые позиции, улучшение |
| Gemini battles | 10-20% | Валидация, adversarial |

### Загрузка человеческих данных

#### Формат данных

Данные должны быть в JSON формате:

```json
{
  "game_id": "tournament_001",
  "source": "human_tournament",
  "moves": [
    {
      "move_number": 1,
      "player": "white",
      "action": 8,  // 0-8, индекс лунки
      "board_before": {
        "white_pits": [9,9,9,9,9,9,9,9,9],
        "black_pits": [9,9,9,9,9,9,9,9,9],
        "white_kazan": 0,
        "black_kazan": 0
      }
    }
  ],
  "result": {
    "winner": "white",
    "final_score": [82, 80]
  }
}
```

#### API для загрузки данных

```bash
# Загрузить один файл
curl -X POST "http://localhost:8000/api/data/upload" \
  -F "file=@games.json" \
  -F "source_type=human_tournament"

# Загрузить директорию
curl -X POST "http://localhost:8000/api/data/import-folder" \
  -H "Content-Type: application/json" \
  -d '{"folder_path": "data/human_games", "source_type": "human_tournament"}'
```

### Конвертация данных

Скрипты для конвертации разных форматов:

```bash
# Конвертировать дебютную книгу
python scripts/parse_opening_book.py --input open_tree2.txt --output training_data/

# Конвертировать турнирные записи
python scripts/parse_tournament_games.py --input games.txt --output training_data/

# Конвертировать PlayOK PGN
python scripts/parse_playok_pgn.py --input all_results_combined.txt --output training_data/
```

---

## 📈 Мониторинг прогресса

### Метрики на вкладке "Аналитика"

| Метрика | Описание | Хорошее значение |
|---------|----------|------------------|
| Winrate vs Gemini | % побед против Gemini | >50% |
| ELO | Текущий рейтинг | >1500 |
| Loss | Ошибка обучения | ↓ со временем |
| Convergence | Статус сходимости | "improving" |

### ELO Категории

| ELO | Категория |
|-----|-----------|
| <1200 | Начинающий |
| 1200-1400 | Любитель |
| 1400-1600 | Клубный игрок |
| 1600-1800 | Кандидат |
| 1800-2000 | Мастер |
| 2000-2200 | Международный мастер |
| >2200 | Гроссмейстер |

### Weights & Biases интеграция

Для продвинутого мониторинга:

```python
# Установка
pip install wandb

# Авторизация
wandb login

# В коде
from backend.wandb_integration import wandb_tracker

wandb_tracker.start_run()
# ... обучение ...
wandb_tracker.finish_run()
```

---

## 🔧 Troubleshooting

### Проблема: Сервер не запускается

```bash
# Проверить порт
netstat -an | findstr 8000

# Убить процесс Python
taskkill /F /IM python.exe

# Перезапустить
python run.py
```

### Проблема: Gemini API ошибки

```bash
# Проверить API ключ
echo $GEMINI_API_KEY

# Установить ключ
export GEMINI_API_KEY="your-key-here"

# Или в .env файле
echo "GEMINI_API_KEY=your-key" > .env
```

### Проблема: Тренировка зависает

1. Проверьте логи: `logs/training/`
2. Уменьшите batch_size
3. Перезапустите сервер

### Проблема: Низкий winrate

1. Увеличьте количество игр self-play
2. Добавьте человеческие данные
3. Уменьшите epsilon (меньше случайных ходов)

---

## ❓ FAQ

### Q: Сколько игр нужно для заметного улучшения?
**A:** Минимум 500 игр self-play + 50 игр против Gemini. Для серьезного улучшения: 5000+ игр.

### Q: Как сохранить модель?
**A:** Модель сохраняется автоматически после каждой сессии в `models/`. Для ручного сохранения:
```bash
curl -X POST "http://localhost:8000/api/model/save?name=my_model_v1"
```

### Q: Как загрузить модель?
**A:** 
```bash
curl -X POST "http://localhost:8000/api/model/load?name=my_model_v1"
```

### Q: Почему модель проигрывает Gemini?
**A:** Gemini - это LLM с огромной базой знаний. Ваша модель:
1. Нуждается в большем количестве данных
2. Должна видеть разнообразные позиции
3. Требует KL-регуляризации для стабильности

### Q: Как добавить свои турнирные данные?
**A:** 
1. Конвертируйте в JSON формат (см. выше)
2. Загрузите через API или положите в `data/human_games/`
3. Перезапустите сервер

### Q: Как проверить качество данных?
**A:**
```bash
curl "http://localhost:8000/api/metrics/all"
```
Это покажет:
- Количество игр по источникам
- Количество transitions для обучения
- Статус датасета

### Q: Можно ли обучать на GPU?
**A:** Да! PyTorch автоматически использует CUDA если доступно:
```python
import torch
print(torch.cuda.is_available())  # Должно быть True
print(torch.cuda.get_device_name(0))  # Название GPU
```

### Q: Как экспортировать обученную модель?
**A:**
```bash
curl "http://localhost:8000/api/model/export?format=onnx" -o model.onnx
```

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `logs/`
2. Посмотрите вкладку "Аналитика" для метрик
3. Создайте issue на GitHub

---

*Последнее обновление: Декабрь 2025*

