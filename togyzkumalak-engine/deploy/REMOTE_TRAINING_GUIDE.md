# 🚀 Полное руководство по удалённому обучению AlphaZero на Vast.ai

## Содержание
1. [Обзор архитектуры](#обзор-архитектуры)
2. [Подключение к серверу](#подключение-к-серверу)
3. [Настройка и обновление кода](#настройка-и-обновление-кода)
4. [Запуск бэкенд-сервера](#запуск-бэкенд-сервера)
5. [Блиц-обучение (Результат за 2 часа)](#блиц-обучение-результат-за-2-часа)
6. [Мониторинг и диагностика (Дашборд)](#мониторинг-и-диагностика)
7. [Принудительная остановка и очистка](#принудительная-остановка-и-очистка)
8. [Скачивание результатов](#скачивание-результатов)

---

## Подключение к серверу

### 1. Поднятие SSH-туннеля (Локально)
Обязательно для работы UI по адресу `http://localhost:8000`. Выполнять на своем ПК:
```powershell
ssh -p ПОРТ root@IP_АДРЕС -L 8000:localhost:8000
```

---

## Настройка и обновление кода

### Полная очистка и обновление (в Jupyter)
```python
import subprocess, os, time
os.chdir('/workspace/togyzkumalak/togyzkumalak-engine')
print("🔪 Убиваем старые процессы...")
subprocess.run("pkill -9 -f python", shell=True)
time.sleep(2)
print("📥 Обновляем код...")
subprocess.run(['git', 'checkout', '.'], capture_output=True)
subprocess.run(['git', 'pull', 'origin', 'master'], capture_output=True, text=True)
print("✅ Готово!")
```

---

## Блиц-обучение (Результат за 2 часа)

Этот режим оптимизирован для получения сильной модели в кратчайшие сроки на 16 GPU.

### Параметры Блица:
*   **MCTS Simulations:** 30 (быстрые ходы)
*   **Episodes:** 64 (быстрые итерации)
*   **Parallel Games:** 16 (по 1 игре на каждую RTX 3090)
*   **Iterations:** 24 (хватит на 2 часа)

### Запуск через Python (в Jupyter):
```python
import requests
blitz_config = {
    "numIters": 24,           # 2 часа обучения
    "numEps": 64,             # Игр за итерацию
    "numMCTSSims": 30,        # Скорость обдумывания
    "num_parallel_games": 16, # РАСПРЕДЕЛЕНИЕ ПО 16 GPU
    "use_bootstrap": True     # Продолжить с человеческих данных
}
r = requests.post('http://localhost:8000/api/training/alphazero/start', json=blitz_config)
print(f"🚀 БЛИЦ ЗАПУЩЕН: {r.json()}")
```

---

## Мониторинг и диагностика

### 1. Запуск графического дашборда (Терминал)
Показывает прогресс, графики Loss и загрузку всех 16 видеокарт в реальном времени.
```bash
python /workspace/togyzkumalak/togyzkumalak-engine/deploy/monitor.py
```

### 2. Проверка GPU через nvidia-smi
```bash
nvidia-smi --query-gpu=index,utilization.gpu,memory.used,memory.total --format=csv,noheader,nounits
```

---

## Принудительная остановка и очистка
Если сервер завис или GPU память не освобождается:
1.  **Убить всё:** `pkill -9 -f python`
2.  **Проверить порты:** `fuser -k 8000/tcp`
3.  **Перезапустить сервер:** `python togyzkumalak-engine/run.py`

---

## Скачивание результатов
После завершения (или в процессе) скачай лучшую модель на свой ПК:
```powershell
scp -P ПОРТ root@IP_АДРЕС:/workspace/togyzkumalak/togyzkumalak-engine/models/alphazero/best.pth.tar C:\Downloads\
```

---
*Обновлено: 29.12.2025 (Режим BLITZ + 16 GPU Оптимизация)*
