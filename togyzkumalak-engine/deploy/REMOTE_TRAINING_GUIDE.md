# 🚀 Полное руководство по удалённому обучению AlphaZero на Vast.ai

## Содержание
1. [Обзор архитектуры](#обзор-архитектуры)
2. [Выбор и аренда сервера](#выбор-и-аренда-сервера)
3. [Подключение к серверу](#подключение-к-серверу)
4. [Настройка среды](#настройка-среды)
5. [Запуск сервера](#запуск-сервера)
6. [Запуск обучения](#запуск-обучения)
7. [Мониторинг обучения](#мониторинг-обучения)
8. [Скачивание результатов](#скачивание-результатов)
9. [Устранение проблем](#устранение-проблем)

---

## Обзор архитектуры

```
┌─────────────────────────┐              ┌─────────────────────────────────┐
│   ЛОКАЛЬНЫЙ КОМПЬЮТЕР   │              │      VAST.AI СЕРВЕР             │
│   (Windows/Mac/Linux)   │              │   (Ubuntu + GPU)                │
│                         │              │                                 │
│   ┌─────────────────┐   │   SSH/HTTP   │   ┌─────────────────────────┐   │
│   │ Браузер с UI    │◄──┼──────────────┼──►│ FastAPI Backend         │   │
│   │ localhost:8000  │   │   туннель    │   │ (порт 8000)             │   │
│   └─────────────────┘   │              │   └─────────────────────────┘   │
│                         │              │              │                  │
│   ┌─────────────────┐   │              │              ▼                  │
│   │ Jupyter Notebook│◄──┼──────────────┼──►┌─────────────────────────┐   │
│   │ (управление)    │   │              │   │ AlphaZero Training      │   │
│   └─────────────────┘   │              │   │ • Bootstrap (человеч.)  │   │
│                         │              │   │ • Self-Play             │   │
│   ┌─────────────────┐   │              │   │ • Neural Network Train  │   │
│   │ Git репозиторий │───┼──────────────┼──►│ • Arena evaluation      │   │
│   │ (код)           │   │   git pull   │   └─────────────────────────┘   │
│   └─────────────────┘   │              │              │                  │
│                         │              │              ▼                  │
│   ┌─────────────────┐   │              │   ┌─────────────────────────┐   │
│   │ Чекпоинты       │◄──┼──────────────┼───│ GPU × 16 (RTX 3090)     │   │
│   │ (скачанные)     │   │   download   │   │ DataParallel            │   │
│   └─────────────────┘   │              │   └─────────────────────────┘   │
└─────────────────────────┘              └─────────────────────────────────┘
```

---

## Выбор и аренда сервера

### Шаг 1: Регистрация на Vast.ai

1. Перейди на https://vast.ai
2. Создай аккаунт
3. Пополни баланс (минимум ~$5-10 для тестов)

### Шаг 2: Выбор конфигурации сервера

**Рекомендуемые характеристики:**

| Параметр | Минимум | Оптимально | Наш выбор |
|----------|---------|------------|-----------|
| GPU | 1× RTX 3090 | 8-16× RTX 3090 | 16× RTX 3090 |
| VRAM | 24 GB | 384 GB | 384 GB |
| RAM | 32 GB | 128+ GB | 516 GB |
| Storage | 50 GB | 200+ GB | 5 TB |
| CUDA | 11.8+ | 12.0+ | 13.0 |

**Критерии выбора:**

1. **GPU тип**: RTX 3090/4090 или A100 (лучшее соотношение цена/производительность)
2. **Количество GPU**: больше = быстрее self-play (параллельные игры)
3. **Reliability**: выбирай 95%+ для стабильности
4. **Ports**: убедись что есть открытые порты или Jupyter

### Шаг 3: Аренда сервера

1. На странице https://cloud.vast.ai/templates/ выбери шаблон:
   - **PyTorch** (рекомендуется)
   - или любой с CUDA

2. Фильтры для поиска:
   ```
   GPU: RTX 3090 или лучше
   Num GPUs: 8-16
   Disk Space: 100+ GB
   ```

3. Нажми **RENT** на подходящем сервере

4. В настройках инстанса:
   - **Docker Image**: `pytorch/pytorch:2.1.0-cuda12.1-cudnn8-runtime`
   - **Disk**: 100+ GB
   - **Launch Mode**: `Jupyter` (рекомендуется для новичков)

---

## Подключение к серверу

### Вариант A: Через Jupyter (РЕКОМЕНДУЕТСЯ для новичков)

1. После запуска инстанса, нажми **Open** → **Jupyter**
2. Откроется Jupyter Notebook в браузере
3. Создай новый терминал: **New** → **Terminal**
4. Или выполняй команды прямо в ячейках notebook

### Вариант B: Через SSH

1. **Скопируй SSH команду** из Vast.ai:
   ```
   ssh -p 23396 root@151.237.25.234 -L 8000:localhost:8000
   ```

2. **Если ошибка "Permission denied"**:
   
   Сгенерируй SSH ключ (PowerShell):
   ```powershell
   ssh-keygen -t rsa -b 4096 -f "$env:USERPROFILE\.ssh\id_rsa_vastai"
   ```
   
   Скопируй публичный ключ:
   ```powershell
   Get-Content "$env:USERPROFILE\.ssh\id_rsa_vastai.pub"
   ```
   
   Добавь его на Vast.ai:
   - Account → SSH Keys → Add Key

3. **Подключись с туннелем**:
   ```powershell
   ssh -p ПОРТ root@IP_АДРЕС -L 8000:localhost:8000 -i ~/.ssh/id_rsa_vastai
   ```

---

## Настройка среды

### Одной командой (копируй и вставляй в терминал):

```bash
cd /workspace && \
git clone https://github.com/YOUR_USERNAME/togyzkumalak.git && \
cd togyzkumalak/togyzkumalak-engine && \
pip install -r requirements.txt && \
mkdir -p models/alphazero training_data logs && \
echo "✅ Установка завершена!"
```

### Если репозиторий уже клонирован — обнови:

```bash
cd /workspace/togyzkumalak/togyzkumalak-engine && \
git checkout . && \
git clean -fd && \
git pull origin master && \
pip install -r requirements.txt && \
echo "✅ Обновление завершено!"
```

### Загрузка данных для обучения

Файл `transitions_compact.jsonl` (~50,000 примеров человеческих игр) нужно загрузить отдельно, т.к. он большой:

**Через UI:**
1. Открой UI в браузере
2. Вкладка **Тренировка** → **Training Data**
3. Нажми **Upload** и выбери файл

**Через команду (если файл на сервере):**
```bash
cp /path/to/transitions_compact.jsonl /workspace/togyzkumalak/togyzkumalak-engine/training_data/
```

---

## Запуск сервера

### Из Jupyter Notebook (создай ячейку и выполни):

```python
import subprocess
import sys
import os
import time

# Переходим в директорию проекта
os.chdir('/workspace/togyzkumalak/togyzkumalak-engine')

# Убиваем старые процессы
subprocess.run(['pkill', '-f', 'python run.py'], capture_output=True)
time.sleep(2)

# Запускаем сервер
process = subprocess.Popen(
    [sys.executable, 'run.py'],
    stdout=open('server.log', 'w'),
    stderr=open('server_error.log', 'w')
)

print(f"✅ Сервер запущен! PID: {process.pid}")
print("🌐 Открой в браузере: http://localhost:8000")

# Ждём запуска
time.sleep(5)

# Проверяем здоровье
import requests
try:
    r = requests.get('http://localhost:8000/api/health', timeout=5)
    print(f"✅ Сервер отвечает: {r.json()}")
except:
    print("⏳ Сервер ещё запускается, подожди 10 сек...")
```

### Из терминала SSH:

```bash
cd /workspace/togyzkumalak/togyzkumalak-engine
pkill -f "python run.py" 2>/dev/null
nohup python run.py > server.log 2>&1 &
sleep 5
curl http://localhost:8000/api/health
```

---

## Запуск обучения

### Через UI (рекомендуется):

1. Открой `http://localhost:8000` в браузере
2. Перейди на вкладку **🧠 Тренировка**
3. Разверни панель **AlphaZero - Глубокое Обучение**
4. Нажми **⚡ Авто-конфиг для 16 GPU**
5. Введи количество часов (например, 1)
6. Поставь галочку **Bootstrap с человеческими данными**
7. Нажми **🚀 Запустить AlphaZero**

### Через API (для автоматизации):

```bash
curl -X POST http://localhost:8000/api/training/alphazero/start \
  -H "Content-Type: application/json" \
  -d '{
    "numIters": 192,
    "numEps": 256,
    "numMCTSSims": 200,
    "cpuct": 1.5,
    "hidden_size": 512,
    "num_parallel_games": 16,
    "use_bootstrap": true
  }'
```

### Оптимальные параметры для разных конфигураций:

| GPU | Итераций | Игр/итер | MCTS sims | Batch | Parallel | Время ~1ч |
|-----|----------|----------|-----------|-------|----------|-----------|
| 1×  | 10       | 20       | 50        | 64    | 1        | ✅        |
| 4×  | 48       | 64       | 100       | 256   | 4        | ✅        |
| 8×  | 96       | 128      | 150       | 512   | 8        | ✅        |
| 16× | 192      | 256      | 200       | 1024  | 16       | ✅        |

---

## Мониторинг обучения

### Через UI:

1. Панель **📊 Прогресс Тренировки** показывает:
   - Текущую итерацию
   - Policy/Value Loss графики
   - Win Rate

2. Кнопка **📋 Показать логи** открывает детальные логи

### Через Jupyter (создай ячейку):

```python
import requests
import time
from datetime import datetime

while True:
    try:
        # Статус задачи
        r = requests.get('http://localhost:8000/api/training/alphazero/sessions')
        sessions = r.json()
        
        if sessions:
            s = sessions[-1]  # Последняя сессия
            print(f"""
{'='*60}
⏰ {datetime.now().strftime('%H:%M:%S')}
📋 Task: {s.get('task_id')}
📊 Статус: {s.get('status')}
🔄 Итерация: {s.get('iteration', 0)} / {s.get('total_iterations', '?')}
🎮 Игр: {s.get('games_played', 0)}
📈 Policy Loss: {s.get('policy_loss', 'N/A')}
📉 Value Loss: {s.get('value_loss', 'N/A')}
{'='*60}
            """)
        
        # GPU статус
        import subprocess
        gpu = subprocess.run(['nvidia-smi', '--query-gpu=index,utilization.gpu,memory.used,memory.total', 
                            '--format=csv,noheader,nounits'], capture_output=True, text=True)
        print("🎮 GPU:")
        for line in gpu.stdout.strip().split('\n')[:4]:
            parts = line.split(', ')
            print(f"  GPU {parts[0]}: {parts[1]}% | {parts[2]}/{parts[3]} MiB")
            
    except Exception as e:
        print(f"❌ Ошибка: {e}")
    
    time.sleep(10)
```

### Проверка логов ошибок:

```python
# Последние ошибки
!tail -50 /workspace/togyzkumalak/togyzkumalak-engine/server_error.log | grep -E "(ERROR|Exception|Traceback)"

# Полные логи
!tail -100 /workspace/togyzkumalak/togyzkumalak-engine/server.log
```

---

## Скачивание результатов

### Список чекпоинтов:

```bash
ls -la /workspace/togyzkumalak/togyzkumalak-engine/models/alphazero/
```

### Через UI:

1. Вкладка **🧠 Тренировка** → панель **AlphaZero**
2. Список чекпоинтов с кнопками **📥 Скачать**

### Через SCP (с локального компьютера):

```powershell
# Скачать все чекпоинты
scp -P ПОРТ root@IP_АДРЕС:/workspace/togyzkumalak/togyzkumalak-engine/models/alphazero/*.pth.tar ./downloaded_checkpoints/

# Скачать лучший чекпоинт
scp -P ПОРТ root@IP_АДРЕС:/workspace/togyzkumalak/togyzkumalak-engine/models/alphazero/best.pth.tar ./
```

### Автоматическая синхронизация (PowerShell скрипт):

```powershell
# sync_checkpoints.ps1
$remoteHost = "root@IP_АДРЕС"
$remotePort = "ПОРТ"
$remotePath = "/workspace/togyzkumalak/togyzkumalak-engine/models/alphazero/"
$localPath = "C:\Users\Admin\Documents\Toguzkumalak\checkpoints\"

while ($true) {
    Write-Host "$(Get-Date) - Синхронизация чекпоинтов..."
    scp -P $remotePort "${remoteHost}:${remotePath}*.pth.tar" $localPath
    Start-Sleep -Seconds 300  # Каждые 5 минут
}
```

---

## Устранение проблем

### Проблема: "Address already in use"

```bash
# Найти и убить процесс на порту 8000
lsof -i :8000
kill -9 $(lsof -t -i :8000)

# Или убить все Python процессы
pkill -f "python run.py"
```

### Проблема: "Permission denied (SSH)"

1. Проверь, что SSH ключ добавлен на Vast.ai
2. Используй правильный путь к ключу:
   ```
   ssh -i ~/.ssh/id_rsa_vastai -p ПОРТ root@IP
   ```

### Проблема: UI не открывается

1. Проверь, что SSH туннель активен:
   ```
   ssh -p ПОРТ root@IP -L 8000:localhost:8000
   ```
2. Или используй Jupyter для доступа к UI

### Проблема: Обучение падает с ошибкой

1. Проверь логи:
   ```bash
   tail -100 server_error.log
   ```
2. Перезапусти сервер:
   ```bash
   pkill -f "python run.py"
   python run.py
   ```

### Проблема: GPU не используются

1. Проверь CUDA:
   ```bash
   python -c "import torch; print(torch.cuda.is_available(), torch.cuda.device_count())"
   ```
2. Проверь nvidia-smi:
   ```bash
   nvidia-smi
   ```

---

## Фазы обучения AlphaZero

```
┌────────────────────────────────────────────────────────────────────┐
│                    СТРУКТУРА ОБУЧЕНИЯ                              │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  ФАЗА 1: BOOTSTRAP (если включен)                                  │
│  ════════════════════════════════                                  │
│  • Загрузка человеческих игр (transitions_compact.jsonl)           │
│  • 10 эпох предобучения на человеческих данных                     │
│  • Policy Loss: ~2.2 → ~2.1                                        │
│  • Время: ~5-10 минут                                              │
│  • В мониторинге: Итерация = 0                                     │
│                                                                    │
│                         ↓                                          │
│                                                                    │
│  ФАЗА 2: ОСНОВНОЙ ЦИКЛ ALPHAZERO                                   │
│  ════════════════════════════════                                  │
│  Для каждой итерации (1 → numIters):                               │
│                                                                    │
│    ┌─────────────────────────────────────────────────────────┐     │
│    │  1. SELF-PLAY                                           │     │
│    │     • numEps игр модели против себя                     │     │
│    │     • MCTS для выбора ходов                             │     │
│    │     • Сбор обучающих примеров                           │     │
│    │     • Параллельно на num_parallel_games GPU             │     │
│    └─────────────────────────────────────────────────────────┘     │
│                         ↓                                          │
│    ┌─────────────────────────────────────────────────────────┐     │
│    │  2. TRAINING                                            │     │
│    │     • Обучение нейросети на собранных примерах          │     │
│    │     • Policy Loss + Value Loss                          │     │
│    │     • Несколько эпох                                    │     │
│    └─────────────────────────────────────────────────────────┘     │
│                         ↓                                          │
│    ┌─────────────────────────────────────────────────────────┐     │
│    │  3. ARENA                                               │     │
│    │     • Новая модель vs старая модель                     │     │
│    │     • Если новая лучше → сохраняем                      │     │
│    │     • Иначе откатываемся                                │     │
│    └─────────────────────────────────────────────────────────┘     │
│                         ↓                                          │
│    ┌─────────────────────────────────────────────────────────┐     │
│    │  4. CHECKPOINT                                          │     │
│    │     • Сохранение модели: alphazero_iterN.pth.tar        │     │
│    │     • Сохранение метрик                                 │     │
│    └─────────────────────────────────────────────────────────┘     │
│                                                                    │
│                    Повторить для следующей итерации                │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## Быстрый старт (TL;DR)

```bash
# 1. Клонировать репо
cd /workspace
git clone https://github.com/YOUR_USERNAME/togyzkumalak.git
cd togyzkumalak/togyzkumalak-engine

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Запустить сервер
nohup python run.py > server.log 2>&1 &

# 4. Открыть UI
# В браузере: http://localhost:8000

# 5. Запустить обучение через UI
# Вкладка "Тренировка" → "Авто-конфиг" → "Запустить"
```

---

## Полезные команды

```bash
# Статус GPU
nvidia-smi

# Проверка сервера
curl http://localhost:8000/api/health

# Логи в реальном времени
tail -f server.log

# Остановить обучение
curl -X POST http://localhost:8000/api/training/alphazero/stop

# Список моделей
curl http://localhost:8000/api/training/models

# Обновить код с GitHub
git pull origin master && pkill -f "python run.py" && nohup python run.py > server.log 2>&1 &
```

---

## Контакты и ресурсы

- **GitHub**: https://github.com/YOUR_USERNAME/togyzkumalak
- **Vast.ai**: https://vast.ai
- **PyTorch**: https://pytorch.org/docs/stable/index.html

---

*Последнее обновление: Декабрь 2024*
